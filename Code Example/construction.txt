main()
└── main_flow (span)
    ├── authenticate_user()
    │   └── verify_token()
    ├── load_user_profile()
    │   └── fetch_from_db()
    └── process_data()
        ├── validate_input()
        └── risky_operation() 
